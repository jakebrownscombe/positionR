% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2.0_function_particle_filter.R
\name{create_de_lookup}
\alias{create_de_lookup}
\title{Create Detection Efficiency Lookup Grid}
\usage{
create_de_lookup(
  de_model,
  max_distance,
  min_depth = 0,
  max_depth = 50,
  n_dist = 2000,
  n_depth = 100
)
}
\arguments{
\item{de_model}{A fitted model object (e.g., logistic regression) that predicts
detection probability from \code{dist_m} and \code{depth_m}.}

\item{max_distance}{Numeric. Maximum distance in metres for the lookup grid.}

\item{min_depth}{Numeric. Minimum depth in metres (default 0).}

\item{max_depth}{Numeric. Maximum depth in metres (default 50).}

\item{n_dist}{Integer. Number of distance bins (default 2000).}

\item{n_depth}{Integer. Number of depth bins (default 100).}
}
\value{
A list with components:
\item{grid}{Matrix of detection probabilities (n_dist x n_depth)}
\item{dist_step}{Distance bin width}
\item{depth_step}{Depth bin width}
\item{min_depth}{Minimum depth value}
\item{max_distance}{Maximum distance}
\item{n_dist}{Number of distance bins}
\item{n_depth}{Number of depth bins}
}
\description{
Pre-computes a dense grid of detection efficiency values from a DE model,
enabling fast array-indexed lookups instead of repeated \code{stats::predict()} calls.
}
