% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2.0_function_particle_filter.R
\name{particle_filter_positioning}
\alias{particle_filter_positioning}
\title{Particle Filter Positioning for Acoustic Telemetry}
\usage{
particle_filter_positioning(
  detection_data,
  station_info,
  de_model,
  raster,
  n_particles = 1000,
  step_length_mean = 50,
  step_length_sd = 30,
  turning_angle_sd = 45,
  time_step = 180,
  max_distance = 30000,
  fish_id_col = "path_id",
  time_col = "datetime",
  station_col = "station_id",
  ess_threshold = 0.5,
  position_method = "mean",
  return_particles = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{detection_data}{Data frame of detections with columns for fish ID, time,
station ID, and detection status.}

\item{station_info}{Data frame or sf object with station coordinates and depths.}

\item{de_model}{A fitted detection efficiency model.}

\item{raster}{A RasterLayer defining the study area. NA cells are barriers.}

\item{n_particles}{Integer. Number of particles per fish (default 1000).}

\item{step_length_mean}{Numeric. Mean step length in metres per time_step seconds (default 50).}

\item{step_length_sd}{Numeric. SD of step length (default 30).}

\item{turning_angle_sd}{Numeric. SD of turning angle in degrees (default 45).}

\item{time_step}{Numeric. Reference time step in seconds for movement scaling (default 180).}

\item{max_distance}{Numeric. Maximum DE lookup distance in metres (default 30000).}

\item{fish_id_col}{Character. Name of fish ID column (default "path_id").}

\item{time_col}{Character. Name of time column (default "datetime").}

\item{station_col}{Character. Name of station ID column (default "station_id").}

\item{ess_threshold}{Numeric. ESS threshold for resampling as fraction of n_particles (default 0.5).}

\item{position_method}{Character. Method for computing point position estimates:
\code{"mean"} (default) uses the weighted mean of particle positions, snapped
to the nearest valid particle if it lands on land; \code{"map"} uses the
highest-density raster cell with continuity penalty to avoid track jumping.}

\item{return_particles}{Logical. Return full particle histories (default FALSE).}

\item{verbose}{Logical. Print progress (default TRUE).}
}
\value{
A list with components:
\item{positions}{Data frame with position estimates per fish/time}
\item{particles}{(Optional) Data frame with all particle positions and weights}
}
\description{
Estimates fish positions using a Sequential Monte Carlo (particle filter) approach.
Particles represent possible fish locations and are propagated forward using a
Correlated Random Walk, then weighted by detection likelihood at each time step.
}
